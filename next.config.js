/** @type {import('next').NextConfig} */
const nextConfig = {
	// images: {
	// 	remotePatterns: {
	// 		protocol: 'http',
	// 		hostname: 'dummyjson.com/'
	// 	}
	// }
}

module.exports = nextConfig
